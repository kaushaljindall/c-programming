#include <stdio.h>
#include <string.h>

int main(){
    char line[] = "My Name is kaushal Jindal"; 
    printf( "%s", line);

    // puts() function -- print string
    puts(line);

   //gets() function
   char str[90];
   gets(str);
}
